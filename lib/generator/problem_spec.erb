require 'spec_helper'
require_problem('<%= problem_name_snake_case %>')

RSpec.describe Problems::<%= problem_name_constantized %> do
  let(:subject) { described_class.solve(*args) }

  describe 'solve' do
    context 'TODO' do
      let(:args) { 'TODO' }
      let(:result) { nil }
      
      xit { expect(subject).to eq result }
    end
  end
end